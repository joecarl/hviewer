{
	"name": "hviewer-frontend",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite --host 0.0.0.0",
		"build": "npm run postinstall && tsc && vite build",
		"preview": "vite preview",
		"postinstall": "chispa-cli --compile-html"
	},
	"dependencies": {
		"chispa": "^1.0.1",
		"hls.js": "^1.6.16",
		"lucide-static": "^1.20.0"
	},
	"devDependencies": {
		"typescript": "^7.0.2",
		"vite": "^8.0.16"
	}
}