# Port the backend listens on (default: 8945)
PORT=8945

# Path to the folder containing video files (recursive scan)
VIDEO_PATH=/videos

# Optional: override ffmpeg/ffprobe binary paths (defaults to system PATH)
# FFMPEG_PATH=/usr/local/bin/ffmpeg
# FFPROBE_PATH=/usr/local/bin/ffprobe
